#ifndef SUPPORTIVE_H
#define SUPPORTIVE_H

#include <ctype.h>

#include "struct.h"

int Alpha_index_structs(string str);
int Back_Alpha_index_structs(string str);
bool if_null(char x);

#endif // SUPPORTIVE_H
